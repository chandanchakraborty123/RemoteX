# RemoteX PC Agent (stub)

Future desktop companion that receives commands from the mobile app.

Planned capabilities:
- Lock / sleep / shutdown
- Media keys
- App launch
- Touchpad / keyboard relay

Possible stack later: FastAPI + WebSocket + PyAutoGUI + OS APIs
